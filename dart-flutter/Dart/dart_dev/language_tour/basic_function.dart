// Define a function
printInteger(int aNumber) {
  print('The number is $aNumber.'); // print to console
}

// This is where the app starts exxecuting
main() {
  var number = 42;  // Declare and initializ a variable.
  printInteger(number);
}