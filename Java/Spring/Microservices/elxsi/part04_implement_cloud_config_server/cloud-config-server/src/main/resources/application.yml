server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/christyjohn/config-server-msdemo4Elxsi
          clone-on-start: true